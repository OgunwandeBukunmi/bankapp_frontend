import React from 'react';
import { motion } from 'framer-motion';
import logo from '../assets/logoIcon.png'; // Import your logo

const Loader = () => {
  return (
   
        <div class="spinner-container">
            <div class="spinner"></div>
        </div>
  );
};

export default Loader;
